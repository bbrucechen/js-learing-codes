// 使用两个空格作为缩进
function add () {
  const a = 1;
}

// 花括号前加一个空格

// 在if语句的花括号前也加空格
if(true) {
  console.log(1);
}

// 使用空格把运算符隔开
a = b + c;

// 使用长链语句时用.强调这是方法调用而不是新语句，且要换行
$('#items')
  .find('.selected')
    .highlight()
    .end()
  .find('.open')
    .updateCount();

// 在块末和新语句前插入空行。
if (foo) {
  return bar;
}

return baz;